#!/bin/sh

# Get new mail

string='MAIL'
mail_dir="$HOME/.local/share/isync"
mbox='mailbox'
tuw='tuw'

get_mail(){
	mbox_new=$(find "$mail_dir/$mbox/INBOX/new/" -type f | wc -l)
	tuw_new=$(find "$mail_dir/$tuw/INBOX/new/" -type f | wc -l)

	echo "$string $mbox_new $tuw_new"

}

get_mail
